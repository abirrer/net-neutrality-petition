<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Protect Net Neutrality</title>
        <link rel="stylesheet" href="/stylesheet.css">
    </head>
    <body>
        <header id="signers-header">
            <h2>Edit Your Profile</h1>
        </header>

        <div id="edit-form">
            <form method="POST">
                <input type="hidden" name="_csrf" value="{{csrfToken}}">
                <p>First Name: </p><input type="text" name="first" value="{{first}}">
                <p>Last Name: </p><input type="text" name="last" value="{{last}}">
                <p>Email: </p><input type="email" name="email" value="{{email}}">
                <p>Change Current Password: </p><input type="password" name="password" placeholder="">
                <p>Age: </p><input type="number" name="age" value="{{age}}">
                <p>City: </p><input type="text" name="city" value="{{city}}">
                <p>Personal Website: </p><input type="url" name="website" value="{{website}}">
                <input id="button" type="submit" value="Update">
                <input id="button" action="/logout" name="logout" type="submit" value="Log Out">
            </form>
        </div>

        <script src="http://code.jquery.com/jquery-3.3.1.min.js" integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" crossorigin="anonymous"></script>

    </body>
</html>
